name: keep-render-awake
on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch:
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Check Buzz Status
        run: curl -fsS https://moonshine-tlao.onrender.com/status > /dev/null || true